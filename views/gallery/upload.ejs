<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.0/font/bootstrap-icons.css">
    <link rel="stylesheet" href="/css/theme.css">
    <style>
    .upload-area {
        border: 2px dashed #dee2e6;
        border-radius: 10px;
        padding: 3rem;
        text-align: center;
        transition: all 0.3s;
        background: #f8f9fa;
    }
    .upload-area:hover {
        border-color: #0d6efd;
        background: #e7f1ff;
    }
    .upload-area.dragover {
        border-color: #0d6efd;
        background: #e7f1ff;
    }
    .preview-image {
        max-width: 100%;
        max-height: 300px;
        border-radius: 8px;
        display: none;
    }

    /* Dark theme support */
    [data-theme="dark"] .upload-area {
        border-color: #495057;
        background: #343a40;
    }
    [data-theme="dark"] .upload-area:hover {
        border-color: #0d6efd;
        background: #1a365d;
    }
    [data-theme="dark"] .upload-area.dragover {
        border-color: #0d6efd;
        background: #1a365d;
    }
    [data-theme="dark"] .upload-area h5 {
        color: #e9ecef !important;
    }
    [data-theme="dark"] .upload-area p {
        color: #adb5bd !important;
    }
    [data-theme="dark"] .upload-area .text-muted {
        color: #adb5bd !important;
    }
    [data-theme="dark"] .upload-area .bi-cloud-arrow-up {
        color: #adb5bd !important;
    }
</style>
</head>
<body>
    <%- include('../partials/header') %>
    
    <div class="container mt-4">
        <div class="row justify-content-center">
            <div class="col-lg-8">
                <div class="card">
                    <div class="card-header">
                        <h2 class="card-title mb-0">
                            <i class="bi bi-cloud-upload"></i> Upload Photo
                        </h2>
                    </div>
                    <div class="card-body">
                        <% if (error) { %>
                            <div class="alert alert-danger">
                                <i class="bi bi-exclamation-triangle"></i> <%= error %>
                            </div>
                        <% } %>

                        <form id="uploadForm" action="/gallery/upload" method="POST" enctype="multipart/form-data">
                            <!-- Photo Upload -->
                            <div class="mb-4">
                                <label class="form-label fw-bold">Select Photo *</label>
                                <div class="upload-area" id="uploadArea">
                                    <i class="bi bi-cloud-arrow-up display-4 text-muted mb-3"></i>
                                    <h5>Drag & Drop your photo here</h5>
                                    <p class="text-muted">or click to browse</p>
                                    <input type="file" name="photo" id="photoInput" accept="image/*" class="d-none" required>
                                    <button type="button" class="btn btn-primary mt-2" onclick="document.getElementById('photoInput').click()">
                                        Choose File
                                    </button>
                                </div>
                                <div class="mt-3 text-center">
                                    <img id="preview" class="preview-image" alt="Preview">
                                </div>
                                <div class="form-text">
                                    Supported formats: JPG, PNG, GIF. Max size: 5MB
                                </div>
                            </div>

                            <!-- Photo Details -->
                            <div class="mb-3">
                                <label for="title" class="form-label">Title</label>
                                <input type="text" class="form-control" id="title" name="title" 
                                       value="<%= formData.title || '' %>" 
                                       placeholder="Give your photo a title...">
                            </div>

                            <div class="mb-3">
                                <label for="description" class="form-label">Description</label>
                                <textarea class="form-control" id="description" name="description" 
                                          rows="3" placeholder="Describe your photo..."><%= formData.description || '' %></textarea>
                            </div>

                            <!-- Pet Selection -->
                            <div class="mb-3">
                                <label for="pet_id" class="form-label">Associated Pet (Optional)</label>
                                <select class="form-select" id="pet_id" name="pet_id">
                                    <option value="">No specific pet</option>
                                    <% pets.forEach(pet => { %>
                                        <option value="<%= pet.pet_id %>" 
                                                <%= (formData.pet_id == pet.pet_id) ? 'selected' : '' %>>
                                            <%= pet.name %> (<%= pet.species %>)
                                        </option>
                                    <% }); %>
                                </select>
                            </div>

                            <!-- Tags -->
                            <div class="mb-3">
                                <label for="tags" class="form-label">Tags (Optional)</label>
                                <input type="text" class="form-control" id="tags" name="tags" 
                                       value="<%= formData.tags || '' %>" 
                                       placeholder="cute, funny, adventure (separate with commas)">
                                <div class="form-text">
                                    Add tags to help others discover your photo
                                </div>
                            </div>

                            <!-- Privacy Settings -->
                            <div class="mb-4">
                                <div class="form-check form-switch">
                                    <input class="form-check-input" type="checkbox" id="is_public" name="is_public" 
                                           <%= (formData.is_public === undefined || formData.is_public) ? 'checked' : '' %>>
                                    <label class="form-check-label fw-bold" for="is_public">
                                        Make this photo public
                                    </label>
                                </div>
                                <div class="form-text">
                                    Public photos are visible to everyone in the community gallery
                                </div>
                            </div>

                            <!-- Submit Buttons -->
                            <div class="d-flex gap-2">
                                <button type="submit" class="btn btn-primary flex-fill">
                                    <i class="bi bi-cloud-upload"></i> Upload Photo
                                </button>
                                <a href="/gallery" class="btn btn-outline-secondary">
                                    <i class="bi bi-arrow-left"></i> Cancel
                                </a>
                            </div>
                        </form>
                    </div>
                </div>
            </div>
        </div>
    </div>
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
    <script>
        // File upload preview and drag & drop
        const uploadArea = document.getElementById('uploadArea');
        const photoInput = document.getElementById('photoInput');
        const preview = document.getElementById('preview');
        const form = document.getElementById('uploadForm');

        // Click to select file
        uploadArea.addEventListener('click', () => {
            photoInput.click();
        });

        // Drag and drop functionality
        ['dragenter', 'dragover', 'dragleave', 'drop'].forEach(eventName => {
            uploadArea.addEventListener(eventName, preventDefaults, false);
        });

        function preventDefaults(e) {
            e.preventDefault();
            e.stopPropagation();
        }

        ['dragenter', 'dragover'].forEach(eventName => {
            uploadArea.addEventListener(eventName, () => uploadArea.classList.add('dragover'), false);
        });

        ['dragleave', 'drop'].forEach(eventName => {
            uploadArea.addEventListener(eventName, () => uploadArea.classList.remove('dragover'), false);
        });

        uploadArea.addEventListener('drop', handleDrop, false);

        function handleDrop(e) {
            const dt = e.dataTransfer;
            const files = dt.files;
            photoInput.files = files;
            handleFiles(files);
        }

        photoInput.addEventListener('change', function() {
            handleFiles(this.files);
        });

        function handleFiles(files) {
            if (files.length > 0) {
                const file = files[0];
                
                // Validate file type
                if (!file.type.match('image.*')) {
                    alert('Please select an image file (JPG, PNG, GIF)');
                    return;
                }

                // Validate file size (5MB)
                if (file.size > 5 * 1024 * 1024) {
                    alert('File size must be less than 5MB');
                    return;
                }

                // Show preview
                const reader = new FileReader();
                reader.onload = function(e) {
                    preview.src = e.target.result;
                    preview.style.display = 'block';
                    uploadArea.style.display = 'none';
                };
                reader.readAsDataURL(file);
            }
        }

        // Form validation
        form.addEventListener('submit', function(e) {
            if (!photoInput.files.length) {
                e.preventDefault();
                alert('Please select a photo to upload');
                return;
            }

            const file = photoInput.files[0];
            if (file.size > 5 * 1024 * 1024) {
                e.preventDefault();
                alert('File size must be less than 5MB');
                return;
            }
        });

        // Reset preview if user wants to change file
        document.addEventListener('click', function(e) {
            if (e.target.id === 'photoInput') {
                preview.style.display = 'none';
                uploadArea.style.display = 'block';
            }
        });
    </script>
</body>
</html>